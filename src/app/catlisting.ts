export interface CatListing {
    id: number;
    name: string;
    photo: string;
    gender: string;
    age: number; // in weeks
    color: string; //blue, chocolate , lilac, red, cream
    pattern: string; //bi-colour, seal, mitted, lynx
    status: string; //reserved, available, sold,
}
